<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs') %>
  <body>
    <%- include('../partials/nav.ejs') %>

    <div class="blogs content">
      <div class="info">
        <p>
          Hello Users! EdHelp is an Educational Website which provide various tools and features to its users in order to improve their learning experience. This Website is developed as neccessary Mini Project.
        </p>
      </div>
      <h2>All Articles</h2>
      <% if(blogs.length > 0){ %> <% blogs.forEach(blog => { %>
      <a class="single" href="/blogs/<%= blog._id %>">
        <h3 class="title"><%= blog.title %></h3>
        <p class="snippet"><%= blog.snippet %></p>
      </a>
      <% }) %> <% } else { %>
      <p>No Blogs to display yet!</p>
      <% } %>
    </div>

    <%- include('../partials/footer.ejs') %>
  </body>
</html>
